
SET(CMAKE_FIND_LIBRARY_SUFFIXES ".a")

SET(App_SRCS
	main.cpp
)

ADD_EXECUTABLE(splinter
	${App_SRCS}
)

TARGET_LINK_LIBRARIES(splinter
	splinter_lib
)

SET_TARGET_PROPERTIES(splinter PROPERTIES LINK_SEARCH_START_STATIC 1)
SET_TARGET_PROPERTIES(splinter PROPERTIES LINK_SEARCH_END_STATIC 1)
